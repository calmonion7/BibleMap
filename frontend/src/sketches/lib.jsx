// 투어 장면 스케치 공용 헬퍼 (task#227 표준 확정본 → #228 모듈 분리)
// 선화 규약: stroke=currentColor, pathLength=1(.symbol-draw dash).
// 얼굴: 장면 스케치는 이목구비를 **그린다**(ADR-0025 2026-08-21 개정). 인물·책 인장은 여전히 초상 없음.
// 선 굵기 위계(일괄 굵기 금지): 원경 1.1 · 질감/주름 1.3 · 지면 1.6 · 보조 1.8~2 · 주역 2.4~2.6 · 핵심 3
// — 전역 배율 W=0.55로 렌더 기준 펜 선 두께. 무드: 어두운 장면은 강조색만 금 → 목탄(패널에서 오버라이드).

export const P = { pathLength: 1 }
export const W = 0.55
export const sw = (n, o) => (o != null
  ? { pathLength: 1, strokeWidth: +(n * W).toFixed(2), opacity: o }
  : { pathLength: 1, strokeWidth: +(n * W).toFixed(2) })
// 단계 딜레이(ms) — g 래퍼의 --sym-delay로 자식 stroke에 상속. reduce 모드에선 0.
export const d = (ms, reduce) => ({ '--sym-delay': reduce ? '0ms' : `${ms}ms` })

// ─────────────────────────────────────────────────────────────────────────────
// 장면 저작 규약 — 경계 B1~B6 (task#286 파일럿에서 확정한 정본)
//
// 이 절 하나로 새 장면 저작에 필요한 수치가 전부 해결된다(외부 문서 왕복 불필요).
// 근거: ADR 260821-174855(밀도를 번들·시간보다 우선) · 260821-174856(재생 체류 시간은
//       장면 예산에 종속) · ADR-0024(모션 토큰 — SMIL 초 단위 예외와 단계 경계).
// 강제: backend/scripts/validate_scene_sketch_detail.py (scripts/check.sh에 배선).
//
// 용어
//   장면 밀도(Scene Density) = 한 장면의 stroke 요소 총수(path·circle·ellipse·line·polyline·rect).
//   구조 선(Structural Stroke) = 굵기 ≥ 2.0인 stroke — 사람·사물의 형태를 이루는 선(B7).
//                              <Label>은 글자라 밀도에 세지 않는다.
//   붓질 단계(Draw Step)      = d(ms) 지연을 건 <g> 래퍼 하나. 그 안의 선들이 동시에 그려진다.
//
// 타이밍 모델(index.css .symbol-draw) — 아래 계산의 유일한 근거
//   모든 stroke는 --dur-draw(=1000ms) 동안 그려지고, 시작 시각은 부모 <g>의 --sym-delay다.
//   ⇒ 장면 드로인 총 길이 = max(단계 지연) + 1000ms
//
// 경계
//   B1 밀도       장면당 stroke ≥ 30 (목표 40~45 — 사람·사물을 자세히 그리면 이 정도가 든다)
//   B2 단계 상한  어떤 붓질 단계도 선 3개 초과 금지 — 한 번에 우수수 나타나면 '그려지는' 느낌이 죽는다
//   B3 단계 하한  장면당 단계 수 ≥ ceil(선 수 ÷ 3)
//   B4 시간 예산  드로인 총 길이 ≤ 7,000ms (중앙 목표 4,500~5,500ms)
//                 ⇒ max(단계 지연) ≤ 6,000ms
//   B5 절정 순서  그 장면의 모든 SMIL begin(초 단위) ≥ 드로인 종료 시각
//                 ⇒ begin ≥ (max(단계 지연) + 1000) / 1000 초. 절정은 그림이 완성된 뒤 터진다.
//   B6 종속 관계  450ms(패널 대기, tourSketches.jsx) + 드로인 총 길이 ≤ min(stepDuration)
//                 현행 min(stepDuration) = 8,360ms (useTourPlayback.js, 296정차지 실측)
//                 ⇒ 드로인 총 길이 ≤ 7,910ms — B4(7,000)가 더 엄격하므로 B4를 지키면 자동 충족
//   B7 구조 밀도  굵기 ≥ 2.0인 stroke ≥ 12개 (사람·사물의 구조)
//   B8 배경 상한  반투명(sw의 두 번째 인자 < 1) stroke가 전체의 40% 이하
//   B9 연출 완료  450ms(패널 대기) + 마지막 연출(SMIL·이름표) **종료** 시각 ≤ min(stepDuration)
//                 현행 예산 = 8,360 − 450 = **7,910ms**. B5는 절정이 드로인 *뒤*에 오는지만 보고,
//   B10 배치 분리 이름표(글) 상자에 어떤 stroke도 들어오지 않고, 상자가 프레임 안에 있으며,
//                 이름표끼리도 겹치지 않는다. 판정은 **선분 대 상자 교차**다(바운딩 박스로 재면
//                 가로로 긴 지면선 하나가 그 띠 전체를 덮은 것으로 잡혀 과대평가된다).
//                 사용자 지시(2026-08-21): "그림과 글이 겹치지 않게 배치". 이름표의 종이색
//                 헤일로(SceneLabel.jsx)는 겹침을 **견디는** 장치이고, 겹치지 않는 것과는 다르다.
//   B11 구조 연결 구조 선(굵기 ≥ 2.0)은 다른 구조 선과 **2.0단위 안에서** 만난다.
//                 배경·질감(얇은 선)은 제외 — 구름·새·자갈은 원래 떨어져 있는 것이 정상이다.
//                 사용자 지적(2026-08-21): "예수님 몸과 얼굴이 너무 떨어져있지 않음?",
//                 "다윗의 몸과 다리가 안맞음". 실측하니 8건이 2.4~5.9단위 떠 있었다 —
//                 머리·팔·다리·투구·지팡이를 몸에 **잇지 않고 따로** 그린 결과다.
//                 렌더 배율이 약 3.7px/단위라 3단위 공백은 11px로 그대로 보인다.
//
//   신체 조립 규칙 — B11이 잡는 것보다 넓다(기계로 다 못 잰다)
//     · 머리는 목(짧은 두 선)으로 어깨에 잇는다. 머리와 어깨 사이를 비워 두지 않는다.
//     · 팔은 어깨에서, 다리는 몸통 밑단에서 출발한다 — 시작점이 그 부위와 닿아야 한다.
//     · **다리 중심은 몸통 중심과 1단위 안에서 맞춘다.** 다윗은 몸통 중심 x=30인데 다리가
//       x 26.5·28.5에 몰려(중심 27.5) 2.5단위 치우쳐 붙어 있었다 — 이건 B11이 못 잡는다
//       (다리가 몸통 밑단 한쪽 끝에는 닿아 있으므로). 좌우 대칭과 중심 정렬은 눈으로 확인하라.
//     · 지물(지팡이·창·방패)은 쥔 손에 닿게 한다.
//
//     ── 상자·사다리로 읽히는 실패 (반복 관측, 2026-08-22) ──────────────────────
//     세 장면에서 같은 실패가 반복됐다. 원인이 같으니 규칙으로 못 박는다:
//     · **몸통을 직사각형으로 그리지 말라.** 어깨에서 허리로 **좁아지거나**(또는 겉옷이 밑으로
//       퍼지거나) 해야 사람으로 읽힌다. 위아래 폭이 같은 네 선은 상자다(골리앗이 그랬다).
//     · **몸통을 가로지르는 전폭 가로대를 넣지 말라.** 허리띠·갑주 단은 **짧게**(몸통 폭의 절반
//       이하) 또는 양끝을 띄워 그린다. 전폭 가로대가 둘 이상이면 사다리로 읽힌다
//       (다윗의 튜닉, 모세의 두 돌판이 그랬다).
//     · **다리를 각진 꺾임으로 그리지 말라.** 무릎을 예각으로 꺾으면 마름모 도형이 된다.
//       완만한 곡선이나 살짝 벌린 직선이 다리로 읽힌다.
//     · **머리가 몸통에 비해 너무 작으면 로봇/우편함으로 보인다.** 머리 지름은 어깨 폭의
//       0.45~0.6배를 유지하라(어깨 21이면 머리 지름 9~12).
//     · 판단 기준은 하나다 — **렌더를 보고 "사람인가 물건인가"를 물어라.**
//       `python3 scripts/render_scene.py <SceneName> --at <ms> --out /tmp/x.png`
//     ─────────────────────────────────────────────────────────────────────────
//
//   배치 레시피 — 띠를 나눈다(이걸 지키면 B10은 자동으로 통과한다)
//     · 상단 띠 y ≤ 8    이름표(인물·지명 등 이름표). size 4.6이면 기준선 y=7이 안전하다.
//     · 그림 y 10 ~ 55   모든 stroke는 이 안에 둔다.
//     · 하단 띠 y ≥ 57   서술형 이름표(한 줄 설명). 폭이 넓으니 x는 프레임(0~120) 안으로.
//     이름표 폭 ≈ 글자수 × size. 8자 × 4.6 = 37단위 — 프레임 폭의 31%다. x를 중앙에 두고
//     좌우가 프레임을 넘지 않는지 반드시 계산하라(실제로 x=126까지 나간 이름표가 있었다).
//                 B6은 드로인이 step 안에 끝나는지만 본다 — 그 둘을 지켜도 **절정 자체가 step을 넘겨**
//                 끝나면 관객은 결말을 못 본다. 2차 재작도에서 사무엘의 여유가 90ms로 측정됐다.
//                 `<Label at="Ns">`도 연출이다(0.4s 페이드인) — 함께 센다.
//
// ── B7·B8이 왜 있는가 — B1은 개수만 재고 값어치를 못 잰다 ──────────────────────
// 파일럿 1차 재작도가 B1~B6을 전부 통과했는데도 사람 판정에서 되돌아왔다. 실측이 이유를 말해 준다:
//   사무엘   36선 중 구조 선 3개(8%) · 반투명 29개(81%)
//   변화산   36선 중 구조 선 4개(11%) · 반투명 22개(61%)
//   골리앗   39선 중 구조 선 15개(38%) · 반투명 11개(28%)   ← 원래 제대로 그려져 있던 기준점
// 앞의 둘은 늘어난 선이 대부분 옅은 배경 획(구름·능선·광선·지면 질감)이어서, 선 수는 3.6배인데
// 화면 인상은 그만큼 달라지지 않았다. B7이 구조를 실제로 그리게 하고, B8이 유령 획으로 개수를
// 채우는 길을 막는다. 사용자 지시(2026-08-21): "사람 및 사물을 자세히 그려줘."
//
// ── 기계가 못 재는 축 — 사람을 자세히 그린다는 것 ─────────────────────────────
// B7·B8을 통과하면서도 사람은 성냥개비일 수 있다(골리앗이 그랬다 — 갑주·방패·창은 자세한데
// 다윗은 동그라미 머리 + 단선 몸통). 검증기로 잴 수 없으니 규약으로 못 박는다:
//   · 인물은 **옷의 실루엣**으로 그린다 — 겉옷/튜닉의 외곽선, 허리 매듭, 옷자락의 흐름.
//     막대기 몸통(단선 1개) 금지. 몸통은 최소 2선(앞·뒤 옷선)으로 부피를 준다.
//   · 팔·다리도 부피를 준다(소매·바지통의 두 선). 손은 형태를 암시하고(주먹·펼친 손),
//     발은 신발·발등의 각을 준다. 머리는 두건·머리카락·수염의 **실루엣**으로.
//   · **얼굴 이목구비를 그린다**(ADR-0025 2026-08-21 개정) — 아래 "얼굴" 절의 수치를 지켜라.
//     장면이 무엇을 하는 장면인지는 시선과 표정이 말한다. 자세만으로는 재판과 서 있음이 갈리지 않는다.
//   · 사물도 같은 원칙 — 윤곽 하나로 끝내지 말고 재질·이음·손잡이·그림자면을 준다.
//   · 배경은 장면을 앉히는 최소한으로. 능선·구름·질감으로 개수를 채우면 B8이 잡는다.
//
// 저작 레시피(40~45선 장면의 기본형)
//   선 42개 → 단계 14개 → 지연 0, 350, 700, … 4550ms (간격 350ms)
//   드로인 총 길이 = 4550 + 1000 = 5,550ms ≤ 7,000 ✓ · SMIL begin ≥ 5.6s
//   ⇒ 절정에 쓸 수 있는 시간 = 7,910 − 5,550 = 약 2.4초. 간격을 400ms로 벌리면 드로인이 6,200ms가
//     되어 절정 예산이 1.7초로 줄고, 연출이 두세 박이면 B9에 걸린다. **간격은 350ms가 기본값이다.**
//   그중 구조 선(≥ 2.0) 12개 이상 · 반투명은 17개 이하(42 × 0.4)
//   단계 순서는 의미를 따른다: 지면/원경 → 주역 실루엣 → 지물·소품 → 질감/주름 → 강조.
//
// 불변(이 규약이 바꾸지 않는 것)
//   · 얼굴은 그린다(ADR-0025 개정). 단 아래 수치를 넘기면 이 배율에서 먹덩이가 된다:
//
//     ── 얼굴 ─────────────────────────────────────────────────────────────────
//     · 머리 반지름 **≥ 4단위**. 그보다 작으면 눈·입이 물리적으로 안 들어간다.
//       (굵기 1.3의 실제 폭이 0.72단위다 — 반지름 3인 머리에 눈 둘을 넣으면 절반이 먹이다.)
//     · 이목구비는 **얇은 선만**: 굵기 1.1~1.3. 여기에 2.0 이상을 쓰면 무조건 뭉갠다.
//       (그래서 얼굴 선은 B7의 구조 선으로 세지지 않는다 — 구조 선 12개는 몸·지물에서 채워라.)
//     · 최소 표현: **눈 2 + 입 1**, 필요할 때 눈썹 2. 코·귀·주름은 넣지 않는다.
//       눈은 짧은 점/획(길이 0.6~1.0) 또는 반지름 0.4~0.5 원. 입은 1.5~2단위 호.
//     · 눈 간격은 머리 반지름의 약 0.7배. 입은 눈에서 아래로 반지름의 약 0.6배.
//     · 시선 방향으로 의미를 만든다 — 재판하는 사람은 청원자를 **본다**. 고개 각도와 시선을
//       서사와 맞춰라. 이것이 얼굴을 허용한 이유다.
//     · 신성한 인물(예수 등)은 이목구비를 **최소로** 두고 광휘·자세로 무게를 준다(ADR-0025 개정 단서).
//     ─────────────────────────────────────────────────────────────────────────
//   · 굵기 위계는 위 항목대로, sw()/P 헬퍼로만 굵기를 준다(일괄 굵기 금지).
//   · reduce 모드에선 d()가 0을 반환해 전 단계가 동시 렌더된다 — 단계 수를 늘려도 무해하다.
// ─────────────────────────────────────────────────────────────────────────────

// 장면 이름표 컴포넌트(Label)는 SceneLabel.jsx로 분리 — 이 파일은 순수 헬퍼만(react-refresh 규칙, task#253).
