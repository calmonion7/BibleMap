# 2026-07-03 — 창세기 원시사 얕은 인물 저작 (일괄 승급 2026-08-01)

## 계획 대비 실제

- 계획대로: 원시사 인물 6인 저작·적재.
- **갈린 점 — 적재 에이전트 self-report 오류 2건을 직접 검증으로 교정**:
  1. `versesNull: 14` + "런타임 fetch라 null 정상" → **오진**. ADR-0003(구절 프리베이크)과 모순이고,
     실제 `event_verses`엔 한/영 본문이 정상 베이킹돼 있었다(아마 person_events의 `books`를 세어 null로 오판).
  2. `versesDrilldownOk: false` → Playwright 셀렉터(`[class*='stop']`) 불일치로 자동 클릭이 미도달한 것.
     **기능 결함이 아니었다** — `/event/{id}/verses`가 본문을 서빙함을 직접 확인.
  3. 반환 slug 필드가 `authored-cain-journey`로 오라벨(실제 파일은 `cain.json`) — 무해하나 신뢰 저하 신호.
- **교훈 정밀화**: 이전의 "`books` 키 생략" 교훈은 정확히는 **`books: []`(빈 배열) skip** 문제였고,
  정확히 채운 `books` 키는 베이킹에 무해했다.

## 학습

- **다음에 다르게 할 것**
  - **서브에이전트의 self-report는 증거가 아니다.** 낙관·오진하며, 특히 "정상입니다"라는 판정이 위험하다.
    권위 있는 증거는 **파일·Neo4j·API 직접 실측** 하나뿐이다(task#118에서 이미 나온 교훈의 재확인 — 두 번 나왔다는 건
    검증 설계 자체에 넣어야 한다는 뜻).
  - **에이전트가 "정상"이라고 한 항목일수록 표본 실측하라.** 실패 보고는 대개 사실이지만 성공 보고는 자주 근거가 없다.
  - **자동 검증의 실패는 "기능 결함"과 "검증 스크립트 결함"을 먼저 구분**하라 — 셀렉터 불일치를 결함으로 보고하면
    멀쩡한 기능을 고치려 들게 된다.

## 문서 갱신

- CONTEXT.md 승급: 없음
- ADR 추가: 없음
