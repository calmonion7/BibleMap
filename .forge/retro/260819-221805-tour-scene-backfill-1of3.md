# 2026-07-23 — 투어 장면 스케치 백필 1/3: 창조·족장·출애굽 34장면 (task#240, 일괄 승급 2026-08-19)

## 계획 대비 실제
- 계획대로 된 것: 34장면 순수 추가(기존 165장면 무접촉, git diff 대조), 집합 대조 missing 0, Playwright 83정차지 전량 순회 통과.
- 이탈: 실행 형태를 Dynamic Workflow 대신 **파일당 1병렬 서브에이전트(Agent 도구 3명)** 로.

## 배움
- 다음에 다르게 할 것:
  - **병렬 팬아웃의 단위는 "파일 소유권"이다.** 투어별로 파일이 분리돼 있어(`sketches/<tour>.jsx`) 에이전트 1명당 1파일을 주니 충돌이 원천 차단됐다. 반대로 `curate-relations` 시리즈는 전 파트가 단일 `relations.json`에 append하며 pair 중복을 교차 조율해야 해서 **6파트 전부 직접 순차 실행**해야 했다(병렬이면 서로 덮어쓰고 중복 pair를 만든다). 규모가 아니라 **한 파일을 여러 에이전트가 쓰는가**가 병렬 가능 여부를 정한다.
  - **무인 드라이브(fg-next all·fg-loop)에서는 Agent 팬아웃이 기본 수단이다** — 워크플로우 스크립트 승인이 드라이브를 멈춘다. task#208에서 처음 쓴 뒤 #240·241·242가 같은 근거를 매번 다시 적었다.
  - **화면 진입 경로는 앞선 태스크가 바꿔놓는다.** task#239가 사이트 인트로를 신설한 뒤로 무해시 진입이 인트로에 착지해, 기존 순회 검증 스크립트(`verify_228`)가 그대로는 안 돌았다. `#/tours` 딥링크 + 투어당 새 컨텍스트로 고쳤다. 재사용하는 검증 스크립트는 **진입 경로가 그대로인지부터** 확인한다.
- 확인된 것(유지): `mood: 'dark'` 판정을 "긴장만으로는 미부여, 저주·감옥·죽음·심판에 부여"로 두는 관례가 3부작 내내 일관되게 재현됨(에이전트가 기존 파일을 읽고 답습).

## 문서 반영
- CONTEXT.md 승급: 없음([[장면 스케치]] 등재됨).
- ADR 추가: 없음 — ADR-0029(투어별 JSX 코드 모듈)가 파일 분리 구조를 이미 정본화했고, 그 구조가 곧 병렬 단위였다.
